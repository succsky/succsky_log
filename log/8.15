github仓库太多了，而且里面也没什么东西。把几个仓库合并成一个会好一点。
但是没找到解决办法。主要是有文件夹，不知道怎么弄。现在回想起来，估计是因为没有把文件夹里的 .git 文件删除。
于是就用笨方法了。
1.把库都clone到本地。
2.创建新库，并完成本地的相关操作。
3.在网页上创建新文件夹，pull下来。
4.git add . ; git commit -m"" ;git push
试了一下，可以直接在本地mkdir并git add .
那么我之前没有把.git 文件删除，导致出错。TAT	
